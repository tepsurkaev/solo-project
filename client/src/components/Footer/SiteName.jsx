import { Grid, Typography } from "@material-ui/core";

function SiteName() {
  return (
    <Grid container spacing={10}>
      <Grid item xs={2}>
        <Typography>waterDelivery</Typography>
      </Grid>
    </Grid>
  );
}

export default SiteName;
